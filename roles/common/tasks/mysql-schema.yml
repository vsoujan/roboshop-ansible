- name: Installing the Mysql client
  ansible.builtin.yum:
    name: mysql
    state: installed

- name: Loading the mysql schema
  ansible.builtin.shell: mysql -h mysql.soujandevops.online	-uroot -pRoboShop@1 < /app/schema/{{component}}.sql